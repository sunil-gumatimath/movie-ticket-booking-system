spring:
  application:
    name: movie-ticket-booking-system-api

  datasource:
    url: jdbc:mysql://localhost:3306/movie-ticket-booking-app-db?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver  # ✅ Add this if not already auto-detected

  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
